import React from 'react';

const Banner = () => {
    return (
        <div className={`w-full h-[350px] bg-blue-950 flex justify-center items-center border px-12 py-4 text-center mt-8`}>
            <h1 className={`text-5xl font-bold text-white`}>BANNER / HERO SECTION</h1>
        </div>
    );
};

export default Banner;